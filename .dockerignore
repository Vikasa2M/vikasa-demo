# The image build only needs the Go module (go.mod/go.sum, vendor/, cmd/,
# internal/) to `go build ./cmd/...`. Everything below is irrelevant to that
# and only bloats the build context.
.git
.github
.idea
.playwright-mcp
.claude
docs
demo
deploy
scripts
*.png
*.mp4
*.md
Dockerfile
Makefile
.dockerignore
.gitignore
